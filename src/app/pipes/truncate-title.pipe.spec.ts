import { TruncateTitlePipe } from './truncate-title.pipe';

describe('TruncateTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
